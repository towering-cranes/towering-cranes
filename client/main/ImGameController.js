// module with factory to pass game info
var app = angular.module('ImGameController');

app.factory('', ['$http', 'giantBomb', '$rootScope', function($http, giantBomb, $rootScope) {

  };

  return {

  };
}]);